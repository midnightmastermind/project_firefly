/** ProfileProfile component that takes in props for photo, title, tagline, location, rating, & price (if applicable)
Renders card w/ given style & props */

import React from "react";

const ProfileProfile = ({}) => {
    return (
        <div
            className="profile"
            >
        </div>
    )
}

export default ProfileProfile;