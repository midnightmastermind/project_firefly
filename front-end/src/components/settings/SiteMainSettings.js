
import SiteForm from 'components/site/SiteForm';
import React from 'react';
import { Link } from "react-router-dom";

const SiteMainSettings = (props) => {
    return (
    <div className="list-tools">
        <SiteForm />
    </div>
);
    }

export default SiteMainSettings;