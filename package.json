{
  "name": "project_elron",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV='local' concurrently \"cd ./back-end && npm run server\" \"cd ./front-end && npm start\"",
    "build": "cd back-end && npm run build && cd ../front-end && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
